repos:
  - repo: local
    hooks:
      - id: actionability-framework
        name: Actionability Validation Framework
        entry: python framework_scorer.py
        language: python
        always_run: true
        pass_filenames: false
        additional_dependencies: [pandas, numpy, requests, pynacl]
